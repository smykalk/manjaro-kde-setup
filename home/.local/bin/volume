#!/bin/bash
#
# Set volume with amixer
#

# Increment to use.
incr=5%

case "$1" in
	"up")
        amixer -q sset Master $incr+
	;;
	"down")
        amixer -q sset Master $incr-
	;;
    "toggle")
        amixer -q sset Master toggle 
    ;;
	*)
		echo "Unsupported: \"$1\""
		exit 1
esac
